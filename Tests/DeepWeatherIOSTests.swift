import XCTest
@testable import DeepWeather

final class DeepWeatherIOSTests: XCTestCase {
    func testAppModuleLoads() {
        // Smoke test so the unit-test target has a valid source tree.
        XCTAssertTrue(true)
    }
}

